<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>App Ecommerce - Tercer Entrega</title>
    <link
        href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css"
        rel="stylesheet"
        integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3"
        crossorigin="anonymous"
    />
    <script src="/socket.io/socket.io.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/handlebars.js/4.7.7/handlebars.min.js"></script>
    <link rel="stylesheet" href="./css/style.css">
</head>
<body>
    <header>
        {{> navbar}}
    </header>
    <main class="container-fluid">
        <h1 class="h1 my-4 text-primary text-center">Tu orden fue enviada con éxito</h1>
        <section class="section__bienvenida section">
            <table class="table">
                <thead>
                    <tr>
                        <th scope="col">Producto</th>
                        <th scope="col">Precio</th>
                        <th scope="col">Cantidad</th>
                        <th scope="col">Imagen</th>
                    </tr>
                </thead>
                <tbody>
                    {{#each order.productos }}
                        <tr>
                            <td>{{producto.title}}</td>
                            <td>{{producto.price}}</td>
                            <td>{{quantity}}</td>
                            <td class="w-25">
                                <img src="{{producto.thumbnail}}" class='card-img-top w-60' alt="{{producto.title}}">
                            </td>
                        </tr>
                    {{/each}}
                </tbody>
            </table>
            {{total}}
            <h3>Dirección de entrega: {{order.address}}</h3>
            <h3>DIrreción de email: {{order.email}}</h3>
            <h3>Estado de orden: {{order.status}} N° {{order._id}} </h3>

            <form class="my-4" action="/productos" method="get">
                <button type="submit" class="btn btn-success">Volver a comprar</button>
            </form>
        </section>
    </main>
    <script
        src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p"
        crossorigin="anonymous"
    >
    </script>
    <script src="https://cdn.jsdelivr.net/npm/normalizr@3.6.1/dist/normalizr.browser.min.js"></script>
</body>
</html>